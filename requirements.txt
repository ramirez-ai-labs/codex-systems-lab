# Core ML framework
# torch<2.2 has no wheels for Python 3.12 (3.12 support started at 2.2.0),
# so pip cannot resolve this on newer interpreters. transformers 4.x (pinned
# below) doesn't require torch>=2.4 the way transformers 5.x does, so this
# range covers Python 3.9-3.12 without upgrading transformers.
torch>=2.0,<2.4

# Hugging Face ecosystem
transformers>=4.35,<4.41
datasets>=2.14,<2.19
accelerate>=0.26,<0.30

# Utilities
numpy>=1.23,<2.0
tqdm>=4.64,<5.0

# Notebooks (04_research_reproductions)
jupyter>=1.0,<2.0
matplotlib>=3.7,<4.0
